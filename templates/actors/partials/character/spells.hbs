<div class="spellsContainer">
    <div class="grid grid-2col spellUsesContainer">
        <div class="spellUses">
            <div class="grid grid-11col">
                <label class="grid-span-2">{{localize config.character.spUse}}</label>
                <input type="text" name="system.spellUse.max" value="{{actor.system.spellUse.max}}" data-dtype="Number">
                <input type="checkbox" name="system.spellUse.checks.0" {{#if actor.system.spellUse.checks.[0]}}checked{{/if}} {{#if (lt actor.system.spellUse.max 1)}}disabled{{/if}}>
                <input type="checkbox" name="system.spellUse.checks.1" {{#if actor.system.spellUse.checks.[1]}}checked{{/if}} {{#if (lt actor.system.spellUse.max 2)}}disabled{{/if}}>
                <input type="checkbox" name="system.spellUse.checks.2" {{#if actor.system.spellUse.checks.[2]}}checked{{/if}} {{#if (lt actor.system.spellUse.max 3)}}disabled{{/if}}>
                <input type="checkbox" name="system.spellUse.checks.3" {{#if actor.system.spellUse.checks.[3]}}checked{{/if}} {{#if (lt actor.system.spellUse.max 4)}}disabled{{/if}}>
                <input type="checkbox" name="system.spellUse.checks.4" {{#if actor.system.spellUse.checks.[4]}}checked{{/if}} {{#if (lt actor.system.spellUse.max 5)}}disabled{{/if}}>
                <input type="checkbox" name="system.spellUse.checks.5" {{#if actor.system.spellUse.checks.[5]}}checked{{/if}} {{#if (lt actor.system.spellUse.max 6)}}disabled{{/if}}>
                <input type="checkbox" name="system.spellUse.checks.6" {{#if actor.system.spellUse.checks.[6]}}checked{{/if}} {{#if (lt actor.system.spellUse.max 7)}}disabled{{/if}}>
                <input type="checkbox" name="system.spellUse.checks.7" {{#if actor.system.spellUse.checks.[7]}}checked{{/if}} {{#if (lt actor.system.spellUse.max 8)}}disabled{{/if}}>
            </div>
        </div>
        <div class="spellUseScore">
            <div class="grid grid-8col">
                <label class="grid-span-3">{{localize config.character.classes.sorc}}</label>
                <div class="spellScore">{{actor.system.spellUse.scores.sorc}}</div>
                <label class="grid-span-3">{{localize config.character.classes.prie}}</label>
                <div class="spellScore">{{actor.system.spellUse.scores.prie}}</div>
                <label class="grid-span-3">{{localize config.character.classes.dPri}}</label>
                <div class="spellScore">{{actor.system.spellUse.scores.dPri}}</div>
                <label class="grid-span-3">{{localize config.character.classes.sham}}</label>
                <div class="spellScore">{{actor.system.spellUse.scores.sham}}</div>
                <label class="grid-span-3">{{localize config.character.classes.necro}}</label>
                <div class="spellScore">{{actor.system.spellUse.scores.necro}}</div>
            </div>
        </div>
    </div>
    {{#if (gt actor.system.spellUse.totalSpellsKnown.sorc 0)}}
    <div class="wordSpellsCont spellsHolder">
        <h2 class="spellHeader">{{localize gearConfig.spell.schools.wor}}</h2>
        <div class="spellBlock1 grid grid-2col">
            <h4 class="grid-span-2">{{localize config.common.max}} {{localize config.charSheet.tabs.spell}} {{actor.system.spellUse.totalSpellsKnown.sorc}}</h4>
            <div>
                <div class="grid grid-6col spellLabels">
                    <div></div>
                    <div class="grid-span-4">{{localize config.monster.name}}</div>
                    <div>{{localize config.character.dc}}</div>
                </div>
            </div>
            <div>
                <div class="grid grid-6col spellLabels">
                    <div></div>
                    <div class="grid-span-4">{{localize config.monster.name}}</div>
                    <div>{{localize config.character.dc}}</div>
                </div>
            </div>
            {{#each actor.items as |item id|}}
                {{#if (eq item.type 'spell')}}
                    {{#if (eq item.system.schoolChoice 'Words of True Power')}}
                        <div class="singleSpell genSkillContainer item item-list" data-itemid="{{_id}}" data-actorid="{{actor._id}}" draggable="true" data-type="spell">
                            <div class="grid grid-6col reveal-rollable contextMenu genSkills" data-id="{{_id}}" data-contextType="{{type}}">
                                <img class="spellCast player" data-itemid="{{_id}}" src="{{item.img}}" alt="{{item.name}}">
                                <div class="spellName grid-span-4">{{item.name}}</div>
                                <div class="spellDif">{{item.system.difficulty}}</div>
                                <input type="hidden" id="school" value="{{item.system.schoolChoice}}" class="type">
                                <input type="hidden" id="element" value="{{item.system.elementChoice}}">
                            </div>
                        </div>
                    {{/if}}
                {{/if}}
            {{/each}}
        </div>
    </div>
    {{/if}}

    {{#if (gt actor.system.spellUse.totalSpellsKnown.prie 0)}}
    <div class="miracleSpellsCont spellsHolder">
        <h2 class="spellHeader">{{localize gearConfig.spell.schools.mir}}</h2>
        <div class="spellBlock1 grid grid-2col">
            <h4 class="grid-span-2">{{localize config.common.max}} {{localize config.charSheet.tabs.spell}} {{actor.system.spellUse.totalSpellsKnown.prie}}</h4>
            <div>
                <div class="grid grid-6col spellLabels">
                    <div></div>
                    <div class="grid-span-4">{{localize config.monster.name}}</div>
                    <div>{{localize config.character.dc}}</div>
                </div>
            </div>
            <div>
                <div class="grid grid-6col spellLabels">
                    <div></div>
                    <div class="grid-span-4">{{localize config.monster.name}}</div>
                    <div>{{localize config.character.dc}}</div>
                </div>
            </div>
            {{#each actor.items as |item id|}}
                {{#if (eq item.type 'spell')}}
                    {{#if (eq item.system.schoolChoice 'Miracle')}}
                        <div class="singleSpell genSkillContainer item item-list" data-itemid="{{_id}}" data-actorid="{{actor._id}}" draggable="true" data-type="spell">
                            <div class="grid grid-6col reveal-rollable contextMenu genSkills" data-id="{{_id}}" data-contextType="{{type}}">
                                <img class="spellCast player" data-itemid="{{_id}}" src="{{item.img}}" alt="{{item.name}}">
                                <div class="actorRolls spellName grid-span-4">{{item.name}}</div>
                                <div class="spellDif">{{item.system.difficulty}}</div>
                                <input type="hidden" id="school" value="{{item.system.schoolChoice}}" class="type">
                                <input type="hidden" id="element" value="{{item.system.elementChoice}}">
                            </div>
                        </div>
                    {{/if}}
                {{/if}}
            {{/each}}
        </div>
    </div>
    {{/if}}

    {{#if (gt actor.system.spellUse.totalSpellsKnown.dPri 0)}}
    <div class="dragonSpellsCont spellsHolder">
        <h2 class="spellHeader">{{localize gearConfig.spell.schools.anc}}</h2>
        <div class="spellBlock1 grid grid-2col">
            <h4 class="grid-span-2">{{localize config.common.max}} {{localize config.charSheet.tabs.spell}} {{actor.system.spellUse.totalSpellsKnown.dPri}}</h4>
            <div>
                <div class="grid grid-6col spellLabels">
                    <div></div>
                    <div class="grid-span-4">{{localize config.monster.name}}</div>
                    <div>{{localize config.character.dc}}</div>
                </div>
            </div>
            <div>
                <div class="grid grid-6col spellLabels">
                    <div></div>
                    <div class="grid-span-4">{{localize config.monster.name}}</div>
                    <div>{{localize config.character.dc}}</div>
                </div>
            </div>
            {{#each actor.items as |item id|}}
                {{#if (eq item.type 'spell')}}
                    {{#if (eq item.system.schoolChoice 'Ancestral Dragon')}}
                        <div class="singleSpell genSkillContainer item item-list" data-itemid="{{_id}}" data-actorid="{{actor._id}}" draggable="true" data-type="spell">
                            <div class="grid grid-6col reveal-rollable contextMenu genSkills" data-id="{{_id}}" data-contextType="{{type}}">
                                <img class="spellCast player" data-itemid="{{_id}}" src="{{item.img}}" alt="{{item.name}}">
                                <div class="actorRolls spellName grid-span-4">{{item.name}}</div>
                                <div class="spellDif">{{item.system.difficulty}}</div>
                                <input type="hidden" id="school" value="{{item.system.schoolChoice}}" class="type">
                                <input type="hidden" id="element" value="{{item.system.elementChoice}}">
                            </div>
                        </div>
                    {{/if}}
                {{/if}}
            {{/each}}
        </div>
    </div>
    {{/if}}

    {{#if (gt actor.system.spellUse.totalSpellsKnown.sham 0)}}
    <div class="spiritSpellsCont spellsHolder">
        <h2 class="spellHeader">{{localize gearConfig.spell.schools.spi}}</h2>
        <div class="spellBlock1 grid grid-2col">
            <h4 class="grid-span-2">{{localize config.common.max}} {{localize config.charSheet.tabs.spell}} {{actor.system.spellUse.totalSpellsKnown.sham}}</h4>
            <div>
                <div class="grid grid-6col spellLabels">
                    <div></div>
                    <div class="grid-span-4">{{localize config.monster.name}}</div>
                    <div>{{localize config.character.dc}}</div>
                </div>
            </div>
            <div>
                <div class="grid grid-6col spellLabels">
                    <div></div>
                    <div class="grid-span-4">{{localize config.monster.name}}</div>
                    <div>{{localize config.character.dc}}</div>
                </div>
            </div>
            {{#each actor.items as |item id|}}
                {{#if (eq item.type 'spell')}}
                    {{#if (eq item.system.schoolChoice 'Spirit Arts')}}
                        <div class="singleSpell genSkillContainer item item-list" data-itemid="{{_id}}" data-actorid="{{actor._id}}" draggable="true" data-type="spell">
                            <div class="grid grid-6col reveal-rollable contextMenu genSkills" data-id="{{_id}}" data-contextType="{{type}}">
                                <img class="spellCast player" data-itemid="{{_id}}" src="{{item.img}}" alt="{{item.name}}">
                                <div class="actorRolls spellName grid-span-4">{{item.name}}</div>
                                <div class="spellDif">{{item.system.difficulty}}</div>
                                <input type="hidden" id="school" value="{{item.system.schoolChoice}}" class="type">
                                <input type="hidden" id="element" value="{{item.system.elementChoice}}">
                            </div>
                        </div>
                    {{/if}}
                {{/if}}
            {{/each}}
        </div>
    </div>
    {{/if}}

    {{#if (gt actor.system.spellUse.totalSpellsKnown.necro 0)}}
    <div class="necroSpellsCont spellsHolder">
        <h2 class="spellHeader">{{localize gearConfig.spell.schools.nec}}</h2>
        <div class="spellBlock1 grid grid-2col">
            <h4 class="grid-span-2">{{localize config.common.max}} {{localize config.charSheet.tabs.spell}} {{actor.system.spellUse.totalSpellsKnown.necro}}</h4>
            <div>
                <div class="grid grid-6col spellLabels">
                    <div></div>
                    <div class="grid-span-4">{{localize config.monster.name}}</div>
                    <div>{{localize config.character.dc}}</div>
                </div>
            </div>
            <div>
                <div class="grid grid-6col spellLabels">
                    <div></div>
                    <div class="grid-span-4">{{localize config.monster.name}}</div>
                    <div>{{localize config.character.dc}}</div>
                </div>
            </div>
            {{#each actor.items as |item id|}}
                {{#if (eq item.type 'spell')}}
                    {{#if (eq item.system.schoolChoice 'Necromancy')}}
                        <div class="singleSpell genSkillContainer item item-list" data-itemid="{{_id}}" data-actorid="{{actor._id}}" draggable="true" data-type="spell">
                            <div class="grid grid-6col reveal-rollable contextMenu genSkills" data-id="{{_id}}" data-contextType="{{type}}">
                                <img class="spellCast player" data-itemid="{{_id}}" src="{{item.img}}" alt="{{item.name}}">
                                <div class="actorRolls spellName grid-span-4">{{item.name}}</div>
                                <div class="spellDif">{{item.system.difficulty}}</div>
                                <input type="hidden" id="school" value="{{item.system.schoolChoice}}" class="type">
                                <input type="hidden" id="element" value="{{item.system.elementChoice}}">
                            </div>
                        </div>
                    {{/if}}
                {{/if}}
            {{/each}}
        </div>
    </div>
    {{/if}}
</div>
{{!-- {{log actor.system.spellUse.checks}} --}}
